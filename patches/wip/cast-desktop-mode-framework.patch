diff --git a/core/api/system-current.txt b/core/api/system-current.txt
index a21a30f5b415..38bce179e9fc 100644
--- a/core/api/system-current.txt
+++ b/core/api/system-current.txt
@@ -6710,6 +6710,7 @@ package android.hardware.display {
     method @RequiresPermission(android.Manifest.permission.CONFIGURE_DISPLAY_BRIGHTNESS) public void setBrightnessConfigurationForDisplay(@NonNull android.hardware.display.BrightnessConfiguration, @NonNull String);
     method @Deprecated @RequiresPermission(android.Manifest.permission.CONTROL_DISPLAY_SATURATION) public void setSaturationLevel(float);
     field @FlaggedApi("com.android.server.display.feature.flags.virtual_displays_support_desktop_mode") public static final int VIRTUAL_DISPLAY_FLAG_ALLOWS_CONTENT_MODE_SWITCH = 131072; // 0x20000
+    field @FlaggedApi("com.android.server.display.feature.flags.virtual_displays_support_desktop_mode") public static final int VIRTUAL_DISPLAY_FLAG_CONNECTION_PENDING = 262144; // 0x40000
     field public static final int VIRTUAL_DISPLAY_FLAG_ROTATES_WITH_CONTENT = 128; // 0x80
     field public static final int VIRTUAL_DISPLAY_FLAG_STEAL_TOP_FOCUS_DISABLED = 65536; // 0x10000
     field public static final int VIRTUAL_DISPLAY_FLAG_TRUSTED = 1024; // 0x400
@@ -6717,6 +6718,7 @@ package android.hardware.display {
 
   public final class VirtualDisplayConfig implements android.os.Parcelable {
     method @Nullable public android.view.DisplayCutout getDisplayCutout();
+    method @FlaggedApi("com.android.server.display.feature.flags.virtual_displays_support_desktop_mode") @NonNull public java.util.List<android.view.Display.Mode> getSupportedModes();
     method @FlaggedApi("com.android.server.display.feature.flags.virtual_displays_support_desktop_mode") @Nullable public String getUniqueId();
     method public boolean isHomeSupported();
     method @FlaggedApi("com.android.window.flags.vdm_force_app_universal_resizable_api") public boolean isIgnoreActivitySizeRestrictions();
@@ -6726,6 +6728,7 @@ package android.hardware.display {
     method @NonNull public android.hardware.display.VirtualDisplayConfig.Builder setDisplayCutout(@Nullable android.view.DisplayCutout);
     method @NonNull public android.hardware.display.VirtualDisplayConfig.Builder setHomeSupported(boolean);
     method @FlaggedApi("com.android.window.flags.vdm_force_app_universal_resizable_api") @NonNull public android.hardware.display.VirtualDisplayConfig.Builder setIgnoreActivitySizeRestrictions(boolean);
+    method @FlaggedApi("com.android.server.display.feature.flags.virtual_displays_support_desktop_mode") @NonNull @RequiresPermission(android.Manifest.permission.ADD_TRUSTED_DISPLAY) public android.hardware.display.VirtualDisplayConfig.Builder setSupportedModes(@NonNull java.util.List<android.view.Display.Mode>);
     method @FlaggedApi("com.android.server.display.feature.flags.virtual_displays_support_desktop_mode") @NonNull @RequiresPermission(android.Manifest.permission.ADD_TRUSTED_DISPLAY) public android.hardware.display.VirtualDisplayConfig.Builder setUniqueId(@Nullable String);
   }
 
diff --git a/core/java/android/hardware/display/DisplayManager.java b/core/java/android/hardware/display/DisplayManager.java
index ce3cfd9fc85d..f2ad0bf8a278 100644
--- a/core/java/android/hardware/display/DisplayManager.java
+++ b/core/java/android/hardware/display/DisplayManager.java
@@ -225,6 +225,7 @@ public final class DisplayManager {
             VIRTUAL_DISPLAY_FLAG_OWN_FOCUS,
             VIRTUAL_DISPLAY_FLAG_STEAL_TOP_FOCUS_DISABLED,
             VIRTUAL_DISPLAY_FLAG_ALLOWS_CONTENT_MODE_SWITCH,
+            VIRTUAL_DISPLAY_FLAG_CONNECTION_PENDING,
     })
     @Retention(RetentionPolicy.SOURCE)
     public @interface VirtualDisplayFlag {}
@@ -556,6 +557,27 @@ public final class DisplayManager {
     @SystemApi
     public static final int VIRTUAL_DISPLAY_FLAG_ALLOWS_CONTENT_MODE_SWITCH = 1 << 17;
 
+    /**
+     * Virtual display flag: Create the display in the connected-but-disabled state, rather than
+     * enabled and composing immediately.
+     *
+     * <p>A display created this way is reported as a pending connection, the same state a
+     * physical display occupies between being plugged in and the user deciding what to do with
+     * it. Nothing is composed to it and no activity is placed on it until
+     * {@link #enableConnectedDisplay(int)} is called - which is what lets the system ask whether
+     * the display should mirror or extend before anything is shown on it.</p>
+     *
+     * <p>Note that this flag requires {@link #VIRTUAL_DISPLAY_FLAG_TRUSTED}; a display the system
+     * would not place activities on has nothing to ask about, so it is ignored otherwise.</p>
+     *
+     * @see #createVirtualDisplay
+     * @hide
+     */
+    @FlaggedApi(com.android.server
+            .display.feature.flags.Flags.FLAG_VIRTUAL_DISPLAYS_SUPPORT_DESKTOP_MODE)
+    @SystemApi
+    public static final int VIRTUAL_DISPLAY_FLAG_CONNECTION_PENDING = 1 << 18;
+
     /** @hide */
     @IntDef(prefix = {"MATCH_CONTENT_FRAMERATE_"}, value = {
             MATCH_CONTENT_FRAMERATE_UNKNOWN,
diff --git a/core/java/android/hardware/display/VirtualDisplayConfig.java b/core/java/android/hardware/display/VirtualDisplayConfig.java
index b6fa05f754ac..182b9cbdced3 100644
--- a/core/java/android/hardware/display/VirtualDisplayConfig.java
+++ b/core/java/android/hardware/display/VirtualDisplayConfig.java
@@ -40,6 +40,7 @@ import android.view.DisplayCutout;
 import android.view.Surface;
 
 import java.util.Collections;
+import java.util.List;
 import java.util.Objects;
 import java.util.Set;
 import java.util.concurrent.Executor;
@@ -70,6 +71,7 @@ public final class VirtualDisplayConfig implements Parcelable {
     private final float mDefaultBrightness;
     private final float mDimBrightness;
     private final IBrightnessListener mBrightnessListener;
+    private final List<Display.Mode> mSupportedModes;
 
     private VirtualDisplayConfig(
             @NonNull String name,
@@ -88,7 +90,8 @@ public final class VirtualDisplayConfig implements Parcelable {
             boolean ignoreActivitySizeRestrictions,
             @FloatRange(from = 0.0f, to = 1.0f) float defaultBrightness,
             @FloatRange(from = 0.0f, to = 1.0f) float dimBrightness,
-            IBrightnessListener brightnessListener) {
+            IBrightnessListener brightnessListener,
+            @NonNull List<Display.Mode> supportedModes) {
         mName = name;
         mWidth = width;
         mHeight = height;
@@ -106,6 +109,7 @@ public final class VirtualDisplayConfig implements Parcelable {
         mDefaultBrightness = defaultBrightness;
         mDimBrightness = dimBrightness;
         mBrightnessListener = brightnessListener;
+        mSupportedModes = supportedModes;
     }
 
     /**
@@ -203,6 +207,20 @@ public final class VirtualDisplayConfig implements Parcelable {
         return mBrightnessListener;
     }
 
+    /**
+     * Returns the modes this display can be shown at, or an empty list when the owner declared
+     * none and the display therefore has only the one it was created with.
+     *
+     * @hide
+     */
+    @SystemApi
+    @NonNull
+    @FlaggedApi(com.android.server.display.feature.flags.Flags
+            .FLAG_VIRTUAL_DISPLAYS_SUPPORT_DESKTOP_MODE)
+    public List<Display.Mode> getSupportedModes() {
+        return mSupportedModes == null ? Collections.emptyList() : mSupportedModes;
+    }
+
     /**
      * Returns the unique identifier for the display. Shouldn't be displayed to the user.
      * @hide
@@ -299,6 +317,7 @@ public final class VirtualDisplayConfig implements Parcelable {
         dest.writeFloat(mDefaultBrightness);
         dest.writeFloat(mDimBrightness);
         dest.writeStrongBinder(mBrightnessListener != null ? mBrightnessListener.asBinder() : null);
+        dest.writeTypedList(mSupportedModes);
     }
 
     @Override
@@ -329,7 +348,8 @@ public final class VirtualDisplayConfig implements Parcelable {
                 && Objects.equals(mDisplayCutout, that.mDisplayCutout)
                 && mDefaultBrightness == that.mDefaultBrightness
                 && mDimBrightness == that.mDimBrightness
-                && Objects.equals(mBrightnessListener, that.mBrightnessListener);
+                && Objects.equals(mBrightnessListener, that.mBrightnessListener)
+                && Objects.equals(mSupportedModes, that.mSupportedModes);
     }
 
     @Override
@@ -339,7 +359,8 @@ public final class VirtualDisplayConfig implements Parcelable {
                 mDisplayIdToMirror, mWindowManagerMirroringEnabled, mDisplayCategories,
                 mRequestedRefreshRate, mIsHomeSupported, mDisplayCutout,
                 mIgnoreActivitySizeRestrictions, mDefaultBrightness, mDimBrightness,
-                mBrightnessListener);
+                mBrightnessListener,
+                mSupportedModes);
         return hashCode;
     }
 
@@ -363,6 +384,7 @@ public final class VirtualDisplayConfig implements Parcelable {
                 + " mIgnoreActivitySizeRestrictions=" + mIgnoreActivitySizeRestrictions
                 + " mDefaultBrightness=" + mDefaultBrightness
                 + " mDimBrightness=" + mDimBrightness
+                + " mSupportedModes=" + mSupportedModes
                 + ")";
     }
 
@@ -384,6 +406,7 @@ public final class VirtualDisplayConfig implements Parcelable {
         mDefaultBrightness = in.readFloat();
         mDimBrightness = in.readFloat();
         mBrightnessListener = IBrightnessListener.Stub.asInterface(in.readStrongBinder());
+        mSupportedModes = in.createTypedArrayList(Display.Mode.CREATOR);
     }
 
     /**
@@ -455,6 +478,7 @@ public final class VirtualDisplayConfig implements Parcelable {
         private float mDefaultBrightness = 0.0f;
         private float mDimBrightness = PowerManager.BRIGHTNESS_INVALID;
         private IBrightnessListener mBrightnessListener = null;
+        private List<Display.Mode> mSupportedModes = Collections.emptyList();
 
         /**
          * Creates a new Builder.
@@ -530,6 +554,29 @@ public final class VirtualDisplayConfig implements Parcelable {
             return this;
         }
 
+        /**
+         * Sets the modes this display can be shown at.
+         *
+         * <p>A virtual display otherwise advertises only the single mode it was created with,
+         * which leaves nothing for the connected-display resolution picker to offer. An owner that
+         * knows the real sink - a cast receiver that has been told the television's panel modes,
+         * say - can declare them here, and the mode with the most pixels becomes the default.</p>
+         *
+         * <p>Note: honoured only on a trusted display. The modes must all be positive in both
+         * dimensions; anything else is dropped.</p>
+         *
+         * @hide
+         */
+        @NonNull
+        @SystemApi
+        @RequiresPermission(Manifest.permission.ADD_TRUSTED_DISPLAY)
+        @FlaggedApi(com.android.server.display.feature.flags.Flags
+                .FLAG_VIRTUAL_DISPLAYS_SUPPORT_DESKTOP_MODE)
+        public Builder setSupportedModes(@NonNull List<Display.Mode> supportedModes) {
+            mSupportedModes = supportedModes;
+            return this;
+        }
+
         /**
          * Sets the id of the display that the virtual display should mirror.
          * @hide
@@ -751,7 +798,8 @@ public final class VirtualDisplayConfig implements Parcelable {
                     mIgnoreActivitySizeRestrictions,
                     mDefaultBrightness,
                     mDimBrightness,
-                    mBrightnessListener);
+                    mBrightnessListener,
+                    mSupportedModes);
         }
     }
 }
diff --git a/services/core/java/com/android/server/display/DisplayDeviceInfo.java b/services/core/java/com/android/server/display/DisplayDeviceInfo.java
index a5eb85f87b80..e3f767d2594a 100644
--- a/services/core/java/com/android/server/display/DisplayDeviceInfo.java
+++ b/services/core/java/com/android/server/display/DisplayDeviceInfo.java
@@ -204,6 +204,14 @@ final class DisplayDeviceInfo {
      */
     public static final int FLAG_ALLOWS_CONTENT_MODE_SWITCH = 1 << 20;
 
+    /**
+     * Flag: The display should be created in the connected-but-disabled state, so that it is
+     * offered as a pending connection rather than composing straight away.
+     *
+     * @see android.hardware.display.DisplayManager#VIRTUAL_DISPLAY_FLAG_CONNECTION_PENDING
+     */
+    public static final int FLAG_CONNECTION_PENDING = 1 << 21;
+
     /**
      * Touch attachment: Display does not receive touch.
      */
@@ -820,6 +828,9 @@ final class DisplayDeviceInfo {
         if ((flags & FLAG_STEAL_TOP_FOCUS_DISABLED) != 0) {
             msg.append(", FLAG_STEAL_TOP_FOCUS_DISABLED");
         }
+        if ((flags & FLAG_CONNECTION_PENDING) != 0) {
+            msg.append(", FLAG_CONNECTION_PENDING");
+        }
         return msg.toString();
     }
 }
diff --git a/services/core/java/com/android/server/display/LogicalDisplayMapper.java b/services/core/java/com/android/server/display/LogicalDisplayMapper.java
index ad113a2513f3..1b6c0f405ca8 100644
--- a/services/core/java/com/android/server/display/LogicalDisplayMapper.java
+++ b/services/core/java/com/android/server/display/LogicalDisplayMapper.java
@@ -1433,6 +1433,12 @@ class LogicalDisplayMapper implements DisplayDeviceRepository.Listener {
             // the display should be disabled as later we will receive a device state update, which
             // will tell us which internal displays should be enabled and which should be disabled.
             display.setEnabledLocked(false);
+        } else if ((device.getDisplayDeviceInfoLocked().flags
+                & DisplayDeviceInfo.FLAG_CONNECTION_PENDING) != 0) {
+            // The owner asked for this display to arrive as a pending connection rather than
+            // composing immediately, so that the system can ask what it should be used for
+            // first. It stays disabled until enableConnectedDisplay resolves that.
+            display.setEnabledLocked(false);
         }
 
         mLogicalDisplays.put(displayId, display);
diff --git a/services/core/java/com/android/server/display/VirtualDisplayAdapter.java b/services/core/java/com/android/server/display/VirtualDisplayAdapter.java
index 0d4ae75b428f..55006db8a39b 100644
--- a/services/core/java/com/android/server/display/VirtualDisplayAdapter.java
+++ b/services/core/java/com/android/server/display/VirtualDisplayAdapter.java
@@ -20,6 +20,7 @@ import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_ALLOW
 import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_ALWAYS_UNLOCKED;
 import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_AUTO_MIRROR;
 import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_CAN_SHOW_WITH_INSECURE_KEYGUARD;
+import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_CONNECTION_PENDING;
 import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_DESTROY_CONTENT_ON_REMOVAL;
 import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_DEVICE_DISPLAY_GROUP;
 import static android.hardware.display.DisplayManager.VIRTUAL_DISPLAY_FLAG_OWN_DISPLAY_GROUP;
@@ -69,6 +70,8 @@ import com.android.server.display.feature.flags.Flags;
 import com.android.server.display.utils.DebugTransactionDetails;
 
 import java.io.PrintWriter;
+import java.util.ArrayList;
+import java.util.List;
 import java.util.concurrent.atomic.AtomicInteger;
 
 /**
@@ -366,6 +369,15 @@ public class VirtualDisplayAdapter extends DisplayAdapter {
         private float mCurrentBrightness;
         private final IBrightnessListener mBrightnessListener;
         private final boolean mHasUniqueId;
+        /**
+         * Every mode the owner declared, each with its own globally unique id.
+         *
+         * Built once, because {@code createMode} allocates a fresh id on every call and the
+         * picker in Settings selects by id - regenerating them on resize would invalidate the
+         * user's choice the moment it was applied. Empty when nothing was declared, which is
+         * every caller that is not deliberately driving a real external sink.
+         */
+        private final List<Display.Mode> mDeclaredModes = new ArrayList<>();
 
         public VirtualDisplayDevice(IBinder displayToken, IBinder appToken,
                 int ownerUid, String ownerPackageName, Surface surface, int flags,
@@ -386,7 +398,13 @@ public class VirtualDisplayAdapter extends DisplayAdapter {
             mDimBrightness = virtualDisplayConfig.getDimBrightness();
             mCurrentBrightness = PowerManager.BRIGHTNESS_INVALID;
             mBrightnessListener = virtualDisplayConfig.getBrightnessListener();
-            mMode = createMode(mWidth, mHeight, getRefreshRate());
+            for (Display.Mode declared : virtualDisplayConfig.getSupportedModes()) {
+                if (declared.getPhysicalWidth() > 0 && declared.getPhysicalHeight() > 0) {
+                    mDeclaredModes.add(createMode(declared.getPhysicalWidth(),
+                            declared.getPhysicalHeight(), getRefreshRate()));
+                }
+            }
+            mMode = modeForLocked(mWidth, mHeight);
             mSurface = surface;
             mFlags = flags;
             mCallback = callback;
@@ -580,13 +598,70 @@ public class VirtualDisplayAdapter extends DisplayAdapter {
                 sendTraversalRequestLocked();
                 mWidth = width;
                 mHeight = height;
-                mMode = createMode(width, height, getRefreshRate());
+                mMode = modeForLocked(width, height);
                 mDensityDpi = densityDpi;
                 mInfo = null;
                 mPendingChanges |= PENDING_RESIZE;
             }
         }
 
+        /**
+         * The declared mode of this size, or a fresh one when the size was never declared.
+         *
+         * Keeps ids stable across a resize: a display that was created from a declared list must
+         * keep reporting those same ids in {@code supportedModes}, or the current mode stops
+         * matching anything in the list Settings is showing.
+         */
+        private Display.Mode modeForLocked(int width, int height) {
+            for (Display.Mode declared : mDeclaredModes) {
+                if (declared.getPhysicalWidth() == width
+                        && declared.getPhysicalHeight() == height) {
+                    return declared;
+                }
+            }
+            return createMode(width, height, getRefreshRate());
+        }
+
+        @Override
+        public boolean setUserPreferredDisplayModeLocked(Display.Mode mode) {
+            if (mDeclaredModes.isEmpty()) {
+                // Nothing was declared, so there is no list to have chosen from and the base
+                // class's "not supported" answer is the honest one.
+                return super.setUserPreferredDisplayModeLocked(mode);
+            }
+            if (mode == null) {
+                // Back to the default, which is the largest thing the owner said the sink can do.
+                Display.Mode largest = defaultModeLocked();
+                resizeLocked(largest.getPhysicalWidth(), largest.getPhysicalHeight(), mDensityDpi);
+                return true;
+            }
+            for (Display.Mode declared : mDeclaredModes) {
+                if (declared.getPhysicalWidth() == mode.getPhysicalWidth()
+                        && declared.getPhysicalHeight() == mode.getPhysicalHeight()) {
+                    // Density is deliberately left alone. It was chosen for the sink rather than
+                    // derived from the frame, and rescaling it here would resize every element on
+                    // screen as a side effect of a resolution change.
+                    resizeLocked(declared.getPhysicalWidth(), declared.getPhysicalHeight(),
+                            mDensityDpi);
+                    return true;
+                }
+            }
+            Slog.w(TAG, "Virtual Display: " + mName + " was asked for an undeclared mode " + mode);
+            return false;
+        }
+
+        /** The largest declared mode, which is what a display with a sink should default to. */
+        private Display.Mode defaultModeLocked() {
+            Display.Mode best = mDeclaredModes.get(0);
+            for (Display.Mode candidate : mDeclaredModes) {
+                long area = (long) candidate.getPhysicalWidth() * candidate.getPhysicalHeight();
+                if (area > (long) best.getPhysicalWidth() * best.getPhysicalHeight()) {
+                    best = candidate;
+                }
+            }
+            return best;
+        }
+
         public void stopLocked() {
             Slog.d(TAG, "Virtual Display: stopping device " + mName);
             setSurfaceLocked(null);
@@ -602,6 +677,7 @@ public class VirtualDisplayAdapter extends DisplayAdapter {
             pw.println("mDisplayIdToMirror=" + mDisplayIdToMirror);
             pw.println("mWindowManagerMirroring=" + mIsWindowManagerMirroring);
             pw.println("mRequestedRefreshRate=" + mRequestedRefreshRate);
+            pw.println("mDeclaredModes=" + mDeclaredModes);
         }
 
         @Override
@@ -614,8 +690,11 @@ public class VirtualDisplayAdapter extends DisplayAdapter {
                 mInfo.height = mHeight;
                 mInfo.modeId = mMode.getModeId();
                 mInfo.renderFrameRate = mMode.getRefreshRate();
-                mInfo.defaultModeId = mMode.getModeId();
-                mInfo.supportedModes = new Display.Mode[] { mMode };
+                mInfo.defaultModeId = mDeclaredModes.isEmpty()
+                        ? mMode.getModeId() : defaultModeLocked().getModeId();
+                mInfo.supportedModes = mDeclaredModes.isEmpty()
+                        ? new Display.Mode[] { mMode }
+                        : mDeclaredModes.toArray(new Display.Mode[0]);
                 mInfo.densityDpi = mDensityDpi;
                 mInfo.xDpi = mDensityDpi;
                 mInfo.yDpi = mDensityDpi;
@@ -631,6 +710,12 @@ public class VirtualDisplayAdapter extends DisplayAdapter {
                 if ((mFlags & VIRTUAL_DISPLAY_FLAG_ALLOWS_CONTENT_MODE_SWITCH) != 0) {
                     mInfo.flags |= DisplayDeviceInfo.FLAG_ALLOWS_CONTENT_MODE_SWITCH;
                 }
+                // Trusted only: a display the system will not place activities on has no
+                // mirror-or-extend question to ask, so pending would strand it disabled.
+                if ((mFlags & VIRTUAL_DISPLAY_FLAG_CONNECTION_PENDING) != 0
+                        && (mFlags & VIRTUAL_DISPLAY_FLAG_TRUSTED) != 0) {
+                    mInfo.flags |= DisplayDeviceInfo.FLAG_CONNECTION_PENDING;
+                }
                 if (mNeverBlank) {
                     mInfo.flags |= DisplayDeviceInfo.FLAG_NEVER_BLANK;
                 }
